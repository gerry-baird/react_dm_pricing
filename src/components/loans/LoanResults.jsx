import React from "react";
import { Box, Card, CardContent, CardHeader, Typography } from "@mui/material";
import { Stack } from "@mui/system";

const LoanResults = () => {
  return (
    <Box flex={2} p={2} sx={{ display: { xs: "none", sm: "block" } }}></Box>
  );
};

export default LoanResults;
